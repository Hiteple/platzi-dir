# Schema
type Course {
	_id: ID!
	title: String!
	teacher: String
	description: String!
	topic: String
}

# Return Schema
type Query {
	# Returns all courses
	courses: [Course]

	# Returns a single course
	course(_id: ID!): [Course]
}
